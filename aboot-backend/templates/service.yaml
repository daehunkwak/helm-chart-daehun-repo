apiVersion: v1
kind: Service
metadata:
  name: {{ include "aboot-backend.fullname" . }}
  labels:
    {{- include "aboot-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aboot-backend.selectorLabels" . | nindent 4 }}
